# Jira Configuration
# Your Jira instance URL (e.g., https://your-company.atlassian.net)
JIRA_URL=https://your-company.atlassian.net

# Your Jira email
JIRA_EMAIL=your-email@company.com

# Your Jira API token (get it from https://id.atlassian.com/manage-profile/security/api-tokens)
JIRA_TOKEN=your-api-token-here

# JQL Query to filter tasks (customize based on your projects and filters)
# Example below limits to multiple roadmap projects and a list of team IDs you provide
JQL_QUERY=project in ("PRODUCT ROADMAPS", "TECHNICAL ROADMAP") AND "Team[Team]" in (TEAM_ID_1, TEAM_ID_2) AND status != Killed AND type = Story ORDER BY priority DESC

# Optional: Jira Board ID for faster sprint fetching
# If not set, sprints will be fetched from issues (slower but works without board ID)
JIRA_BOARD_ID=

# Optional: port for the Flask server (defaults to 5000 if unset)
SERVER_PORT=5000
